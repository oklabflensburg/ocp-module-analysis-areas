"""Immutable host migration history retained for existing database compatibility.

These revisions remain host-chain revisions and are deliberately not registered as
a new module-owned baseline. See ``docs/migration-inventory.md``.
"""
